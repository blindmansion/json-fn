{
  "description": "type coercion evaluator integration",
  "cases": [
    {
      "description": "num: composes with arithmetic after coercion",
      "body": {
        "$return": {
          "$call": "add",
          "$args": [
            { "$call": "num", "$args": ["10"] },
            { "$call": "num", "$args": ["20"] }
          ]
        }
      },
      "expected": 30
    }
  ]
}
