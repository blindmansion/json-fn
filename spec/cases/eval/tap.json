{
  "description": "debug tap",
  "cases": [
    {
      "description": "tap works inside higher-order callbacks",
      "body": {
        "$return": {
          "$call": "map",
          "$args": [
            { "$params": ["x"], "$return": { "$call": "tap", "$args": [{ "$var": "x" }, "item"] } },
            [1, 2, 3]
          ]
        }
      },
      "expected": [1, 2, 3]
    }
  ]
}
