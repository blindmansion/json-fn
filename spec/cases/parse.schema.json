{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "json-fn shorthand parse test suite",
  "description": "Documents the shorthand parser test-suite format consumed from spec/cases/parse.",
  "type": "object",
  "required": ["$schema", "description", "cases"],
  "properties": {
    "$schema": {
      "const": "../parse.schema.json",
      "description": "Identifies the parse-case schema relative to a suite in spec/cases/parse."
    },
    "description": { "type": "string", "description": "A description of the test suite." },
    "comment": { "type": "string", "description": "Optional rationale or maintenance context." },
    "mode": {
      "$ref": "#/$defs/mode",
      "description": "The default parser mode for cases in this suite. Defaults to expression."
    },
    "cases": {
      "type": "array",
      "description": "The parser cases in this suite.",
      "items": { "$ref": "#/$defs/case" }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "mode": { "type": "string", "enum": ["expression", "module"] },
    "case": {
      "type": "object",
      "required": ["description", "source"],
      "properties": {
        "description": { "type": "string", "description": "A description used as the test name." },
        "comment": {
          "type": "string",
          "description": "Optional rationale or maintenance context."
        },
        "source": { "type": "string", "description": "The shorthand source text to parse." },
        "mode": {
          "$ref": "#/$defs/mode",
          "description": "Overrides the suite parser mode for this case."
        },
        "expected": {
          "description": "The exact expected canonical JSON result, including an explicit null."
        },
        "error": {
          "description": "Requires parsing to fail, optionally with portable message and source-position assertions.",
          "oneOf": [{ "const": true }, { "type": "string" }, { "$ref": "#/$defs/error" }]
        }
      },
      "oneOf": [{ "required": ["expected"] }, { "required": ["error"] }],
      "additionalProperties": false
    },
    "error": {
      "type": "object",
      "properties": {
        "messageIncludes": {
          "type": "string",
          "description": "A substring that must occur in the parse error message."
        },
        "at": { "$ref": "#/$defs/position" }
      },
      "anyOf": [{ "required": ["messageIncludes"] }, { "required": ["at"] }],
      "additionalProperties": false
    },
    "position": {
      "type": "object",
      "description": "A one-based Unicode code-point source position.",
      "required": ["line", "column"],
      "properties": {
        "line": { "type": "integer", "minimum": 1 },
        "column": { "type": "integer", "minimum": 1 }
      },
      "additionalProperties": false
    }
  }
}
