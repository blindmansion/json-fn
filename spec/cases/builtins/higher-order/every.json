{
  "$schema": "../../builtin.schema.json",
  "builtin": "every",
  "description": "Test whether every array element matches a unary callback.",
  "cases": [
    {
      "description": "passes every value without an index",
      "args": [
        {
          "$callback": {
            "steps": [
              { "args": [1], "outcome": { "returns": true } },
              { "args": [2], "outcome": { "returns": true } },
              { "args": [3], "outcome": { "returns": true } }
            ]
          }
        },
        [1, 2, 3]
      ],
      "outcome": { "returns": true },
      "observations": { "meter": { "charged": 3 } }
    }
  ]
}
