{
  "$schema": "../../builtin.schema.json",
  "builtin": "isObject",
  "description": "Test whether a value is a plain object, excluding arrays and null.",
  "cases": [
    {
      "description": "returns true for a plain object",
      "args": [{ "a": 1 }],
      "outcome": { "returns": true }
    },
    {
      "description": "returns true for an empty object",
      "args": [{}],
      "outcome": { "returns": true }
    },
    {
      "description": "returns false for an array",
      "args": [[1, 2]],
      "outcome": { "returns": false }
    },
    { "description": "returns false for null", "args": [null], "outcome": { "returns": false } },
    { "description": "returns false for a number", "args": [42], "outcome": { "returns": false } },
    {
      "description": "returns false for a string",
      "args": ["hello"],
      "outcome": { "returns": false }
    },
    {
      "description": "returns false for a boolean",
      "args": [true],
      "outcome": { "returns": false }
    }
  ]
}
