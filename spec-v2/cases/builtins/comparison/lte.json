{
  "$schema": "../../builtin.schema.json",
  "builtin": "lte",
  "description": "Test whether one number is less than or equal to another.",
  "cases": [
    {
      "description": "returns true for equal numbers",
      "args": [2, 2],
      "outcome": { "returns": true }
    }
  ]
}
